Move over mutation code from Item.js
Move over editing freeze code from Item.js





Store item properties as multiple keys, in order to get the atomic operations per-property
Modify mutations
	set property - setnx
	append to string
	list push/pop
	set add/rem
	['zadd', 'i20@books', 81, 1000] <--- This is nicer
	{ id: 20, prop: 'books', op: 'zadd', args: [81, 1000 /* timestamp */] }
	{ zadd: ['i20@books', 81, 1000] } <--- this allows for a direct apply of args

	{
		session: 'sessionKey',
		user: user,
		op: 'zadd',
		args: ['i20@books', 81, 1000 /* timestamp */]
	}

	
	item properties are stored at 		I<item id>@<propName>	e.g. I20@books
	query result sets are stored at		Q<queryJSON>			e.g. Q{\"type\":\"task\"}
	channel names for items are			#I<item id>				e.g. #I20
	channel names for queries are		#Q<queryJSON>			e.g. #Q{\"type\":\"task\"}
	channel names for properties are	#P<propName>			e.g. #Pbooks

Use psubscribe for query, propname and item channels respectively

Get an input field synched across multiple windows
Get queries working
Get reduce working
Get old examples working with new fin, or discard them
Get fan Tasks working with new fin